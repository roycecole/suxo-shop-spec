# 28 - 多語系支援 (Internationalization)

## 異動紀錄
| 版本 | 日期 | 作者 | 說明 |
|---|---|---|---|
| v0.1 | 2026-09-08 | ordinarycas | 初版建立，回應「系統都需要支援繁體中文、英文、日文，主要以中文為主」需求 |
| v0.2 | 2026-09-08 | ordinarycas | 新增 §8 明確排除說明：本文件僅涵蓋文字翻譯，不含幣別/日期/數字格式在地化，回應「分析規格還可以補上什麼」發現的缺口 |
| v0.3 | 2026-09-08 | ordinarycas | §3 新增 3.1 小節，定義 [26-project-structure.md](26-project-structure.md) 已規劃、但本文件先前沒說明內容的共用套件 `SuxoShop.Shared.Translation`，解決 [10-gap-analysis.md](10-gap-analysis.md) §12 已列的缺口 |
| v0.4 | 2026-09-08 | ordinarycas | 修正章節編號從 §6 跳號到 §8 的既有缺漏（原 §8/§9 遞補為 §7/§8），順手發現，非本輪主要異動 |
| v0.5 | 2026-09-09 | ordinarycas | §7、§8 定案「英文/日文版不服務海外客群」（使用者 2026-09-09 確認），幣別維持 TWD 單一計價、金流架構不變，正式鎖定為決策；§8 對應待決議項標記已解決 |
| v0.6 | 2026-09-09 | ordinarycas | §3.1 定案**不採用**共用套件 `SuxoShop.Shared.Translation`：`ecommerce-services` repo 裡 Catalog/CMS/Promotions/Shipping 四個服務已各自獨立實作 `Translation` 表，套件本身從未真的被任何服務接線（見 `ecommerce-services` CHANGELOG「第六輪」），與其為零使用量的套件消耗遷移成本，改為承認四服務各自維護是最終設計；同時定案 zh-Hant 版本**不**落在 `Translation` 表裡（沿用各服務主表原欄位，如 `Product.Name`），解決本節原本沒有逐字說明的一處模糊（使用者 2026-09-09 要求「決定 SuxoShop.Shared.Translation 的去留...不要讓套件與現實各說各話」） |

## 1. 範圍

| 支援語言 | 狀態 |
|---|---|
| 繁體中文（`zh-Hant`） | **預設語言**，所有內容一定要有這個語系版本 |
| 英文（`en`） | 支援，可能有未翻譯內容（見 §4 Fallback 策略） |
| 日文（`ja`） | 支援，可能有未翻譯內容 |

**適用範圍**：電商平台的前台（[07-storefront-requirements.md](07-storefront-requirements.md)）與賣家後台（[08-vendor-admin-requirements.md](08-vendor-admin-requirements.md)）。**ShyeCMS 不在本輪範圍**——`shyecms-admin` 是拾夜科技內部台灣員工使用的工具，暫定維持繁體中文單一語系，若未來有需要另外提出（比照 [27-pwa-and-accessibility.md](27-pwa-and-accessibility.md) §4 排除 ShyeCMS 的慣例）。

## 2. URL 路由策略

採**路徑前綴**（Next.js i18n 路由），與既有 SSG/ISR 需求相容，且對 SEO 友善（各語言各自獨立可索引的網址，符合 [06-ecommerce-platform-architecture.md](06-ecommerce-platform-architecture.md) §5 的 SEO 訴求）：

```
/products/珍珠芭樂-5斤裝        → 預設語言（繁中），不加前綴
/en/products/珍珠芭樂-5斤裝     → 英文
/ja/products/珍珠芭樂-5斤裝     → 日文
```

賣家後台（Vite SPA，無 SEO 需求）改用簡單的語言切換器 + `localStorage` 記住選擇即可，不需要路由前綴。

## 3. 內容翻譯的資料模型策略

多語系內容（商品名稱/描述、分類名稱、CMS 頁面內容等）需要儲存多語系版本。採**獨立翻譯表**模式，而非在每張主表加 `_en`/`_ja` 欄位：

```
Translation
├── EntityType   （如 "Product"、"Category"、"PageSection"）
├── EntityId
├── LocaleCode   （"zh-Hant" / "en" / "ja"）
├── FieldName    （如 "Name"、"Description"）
└── Value
```

**為什麼選這個模式而非每張表加語言欄位**：未來若要新增第四種語言，不需要對每張表做 Migration，只是多寫幾筆 Translation 資料；缺點是查詢時需要多一次 join，但考量到目前規模（單一客戶部署，非高流量），這個代價可接受。

**受影響的服務**（凡是有面向使用者文字內容的服務，都需要查詢 Translation）：

| 服務 | 需要翻譯的欄位 |
|---|---|
| [12-service-catalog.md](12-service-catalog.md) | Product.Name/Description/ShortDescription、Category.Name、Tag.Name |
| [20-service-cms.md](20-service-cms.md) | PageSection.Config 內的文案（Banner 文案、RichText 內容，見 [28](28-i18n.md) 與 Markdown 決策的交互，本文件 §5 有說明） |
| [16-service-promotions.md](16-service-promotions.md) | 優惠券顯示文案（如有） |
| [21-service-shipping.md](21-service-shipping.md) | 物流方式顯示名稱 |

每個需要翻譯欄位的服務，各自的資料庫內建立 `Translation` 表（掛在自己的 schema 下，不是集中式的共用服務），沿用既有「服務各自擁有資料」的原則（[06-ecommerce-platform-architecture.md](06-ecommerce-platform-architecture.md) §4）。

**zh-Hant 不落在 `Translation` 表裡**：`Translation` 表只存放 en/ja 這類「非預設語言」的覆寫值；zh-Hant 版本沿用各服務主表本來就有的欄位（如 `Product.Name`/`Product.Description`），不額外在 `Translation` 表存一筆重複資料。這點 v0.5 前的文字沒有逐字寫明，v0.6 已定案（見下方 §3.1），理由與 §4 Fallback 策略一併說明：查詢時「有翻譯用翻譯、沒有就退回主表原欄位」，不需要 `Translation` 表本身也完備到能單獨撐起 zh-Hant 查詢。

### 3.1 共用套件 `SuxoShop.Shared.Translation`：已決定不採用（v0.6 定案）

[26-project-structure.md](26-project-structure.md) §3.1 曾規劃一個共用 NuGet 套件（`ecommerce-services/shared/SuxoShop.Shared.Translation/`），統一 4 個服務（[12](12-service-catalog.md)、[16](16-service-promotions.md)、[20](20-service-cms.md)、[21](21-service-shipping.md)）的 `Translation` 表結構與「讀取＋Fallback」查詢邏輯。**這個套件的程式碼骨架確實存在於 `ecommerce-services` repo，但從未被任何服務實際接線**——4 個服務一直是各自獨立撰寫 `Translation` 實體與 EF Core Configuration，套件本身只留在 `shared/` 資料夾裡當一份沒有被使用的程式碼。

2026-09-09 定案**不採用**，理由：

- 零使用量：撰寫套件當下 4 個服務都已各自有能動的實作，遷移只是把已經在跑的程式碼換一種寫法，沒有新增任何使用者看得到的能力。
- 套件原本的資料模型假設「zh-Hant 也存一筆進 `Translation` 表」，但 4 個服務的實際實作一致採「zh-Hant 留在主表原欄位」（見上文）——這是套件作者自承的具體化決策，並非規格明文要求，而 4 個服務不約而同的另一種選擇，代表後者才是貼近實際使用情境的方向；若要採用套件就必須先解決這個模型分歧，成本進一步墊高。
- 4 個服務目前各自的 `Translation` 表結構已經彼此高度一致（`EntityType`/`EntityId`/`LocaleCode`/`FieldName`/`Value` + 相同的唯一索引），唯一的例外是 CMS 額外多了 `CreatedAt`/`UpdatedAt` 稽核欄位——維持現狀不影響 §3 描述的資料模型策略本身，只是實作上分散在 4 個服務而非集中在 1 個套件裡。

套件程式碼保留在 repo 內（不刪除），角色降級為**參考範本**：新增第 5 個需要翻譯欄位的服務時，可以參考它的欄位設計與 Fallback 查詢寫法，但不是強制依賴的套件；既有 4 個服務也不會回頭遷移。各服務仍如 §3 所述各自擁有自己的 `Translation` 表與寫入邏輯（`PUT`/`POST` 端點如何更新），這件事從一開始就是每個服務自己的責任，不因套件去留而改變。

## 4. Fallback 策略

以「主要以中文為主」為前提：**任何欄位若該語言沒有翻譯，一律 fallback 到繁體中文**，不顯示空白或原始欄位鍵名。這代表繁體中文（`zh-Hant`）版本是唯一**強制要求**填寫完整的版本，英文/日文允許部分缺漏。

## 5. UI 文字（非使用者輸入內容）

前台/後台介面本身的固定文字（按鈕、標籤、錯誤訊息）採標準 i18n 函式庫的語系檔（JSON），不透過 Translation 表（那是給使用者輸入的動態內容用的）：

| 專案 | 函式庫 |
|---|---|
| 前台（Next.js） | `next-intl` 或 `next-i18next` |
| 後台（Vite SPA） | `react-i18next` |

## 6. 與既有決策的交互影響

- **WooCommerce 匯出**（[08-vendor-admin-requirements.md](08-vendor-admin-requirements.md) §5）：CSV 匯出**只匯出繁體中文（預設語言）版本**，WooCommerce 標準 CSV 格式沒有原生多語系欄位（那需要額外的多語系外掛如 WPML），本輪不處理多語系匯出。
- **Markdown 內容編輯**（見 [08](08-vendor-admin-requirements.md)、[12](12-service-catalog.md)、[20](20-service-cms.md) 的 Markdown 決策）：每個語言版本的 Markdown 內容各自獨立輸入，不是同一份內容機器翻譯，翻譯品質與提供方式見 §8。
- **PWA 快取**（[27-pwa-and-accessibility.md](27-pwa-and-accessibility.md)）：Service Worker 快取 key 需要包含語言前綴，避免不同語言版本的頁面互相快取覆蓋。

## 7. 明確排除：本文件不涵蓋幣別/日期/數字在地化

「多語系」直覺上常常包含幣別、日期、數字格式一起在地化，但本文件**只處理文字翻譯**，以下**明確不在範圍內**，避免日後誤以為已經涵蓋：

| 項目 | 現況 |
|---|---|
| 幣別 | 全平台維持**新台幣（TWD）單一計價**，即使切換到英文/日文介面，價格仍顯示 NT$，不換算成 USD/JPY——英文/日文版**只服務在台的外語使用者**（外籍人士/觀光客），**不服務海外客群**，這是使用者確認鎖定的決策（2026-09-09），不再是待決議假設，見 §8 |
| 金流商付款頁語言 | 綠界/藍新等台灣金流商的付款頁面**本身是繁中介面**（見 [18-service-payment.md](18-service-payment.md)），與商店前台的語言選擇無關——買家用英文/日文瀏覽到結帳最後一步，會跳轉到一個繁中的金流付款頁，體驗不連貫，目前無法改善（金流商未提供多語系頁面的選項不在本平台控制範圍內） |
| 日期/數字格式 | 沿用系統預設格式（如 `2026-09-08`），不依語言別切換成 `Sep 8, 2026` 或 `2026年9月8日` 等在地化格式 |

英文/日文版**不會**觸發幣別轉換（匯率來源、鎖價時機）或跨境金流串接的重新設計——那類擴充只在明確要服務海外客群時才需要，而本輪已確認不需要，見 §8。

## 8. 待決議事項
- [ ] 翻譯內容由誰提供：賣家自己輸入英/日文版本，還是串接機器翻譯 API（如 Google Translate）自動產生初稿再人工校對？目前規格假設**人工輸入**，機器翻譯不在本輪範圍
- [ ] 日文/英文版本的 SEO 優先度是否與繁中版本相同（影響 [06](06-ecommerce-platform-architecture.md) §5 SSG 預產清單是否要依語言分別計算）
- [ ] Notification Service（[23-service-notification.md](23-service-notification.md)）LINE 通知訊息是否需要多語系，目前假設 LINE 官方帳號僅服務台灣市場，維持繁中單一語系
- [ ] 賣家後台是否要求賣家至少填寫繁中版本才能上架（資料完整性檢核）
- [x] ~~英文/日文版是否真的要服務海外客群（見 §7）——這個答案會決定幣別/金流是否需要重新設計，是本文件目前最大的未知數~~——**已解決**：使用者確認（2026-09-09）**不服務海外客群**，英文/日文僅服務在台外語使用者，幣別維持 TWD 單一計價、金流架構不變，正式鎖定為決策，不再是開放項目
